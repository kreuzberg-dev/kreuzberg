// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a55e6742f842bb49eb4899752d788ef93f600c4f264847b90aa66a329e98f4b1
// To regenerate: alef generate
// To verify freshness: alef verify
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Aggregate OCR legibility score for a page, reported by the backend that
/// produced its text.
///
/// This is distinct from `OcrConfidence`, which scores a single detected
/// element (a word or line) using detection/recognition confidence from the OCR
/// engine itself. `PageOcrConfidence` is a page-level summary computed after
/// noise filtering, intended for triage of which pages are worth a closer look,
/// not for comparing OCR engines against each other.
/// </summary>
public sealed record PageOcrConfidence {
  /// <summary>
  /// Aggregate legibility score in `0.0..=1.0`, or `None` when the backend that
  /// produced this page does not report a calibrated legibility scale.
  ///
  /// Backends differ in what their confidence numbers mean (see
  /// `OcrConfidence`'s per-backend normalization), and not every backend maps
  /// onto a 0.0-1.0 legibility scale at all. When a backend has no such
  /// calibrated scale, this is `None` rather than a misleading number, and
  /// scores must never be compared across backends.
  /// </summary>
  [JsonPropertyName("score")]
  public double ? Score { get; init; } = null;

  /// <summary>
  /// Number of words the score was averaged over, AFTER noise filtering.
  ///
  /// A small `word_count` means the average is based on little evidence, so a
  /// high `score` next to a small `word_count` is not representative of the
  /// whole page.
  /// </summary>
  [JsonPropertyName("word_count")]
  public uint WordCount {
    get; init;
  } = 0;

  /// <summary>
  /// Name of the OCR backend that produced the page text.
  /// </summary>
  [JsonPropertyName("backend")]
  public string Backend {
    get; init;
  } = "";

  /// <summary>
  /// Parse a <see cref="PageOcrConfidence"/> from a JSON string.
  /// </summary>
  /// <exception cref="XbergException">When the JSON cannot be
  /// deserialised.</exception>
  public static PageOcrConfidence FromJson(string json) {
    try {
      return JsonSerializer.Deserialize<PageOcrConfidence>(json, JsonOptions) ??
             throw new XbergException(
                 $"Failed to parse PageOcrConfidence from JSON: deserializer returned null");
    } catch (XbergException) {
      throw;
    } catch (Exception e) {
      throw new XbergException(
          $"Failed to parse PageOcrConfidence from JSON: {e.Message}", e);
    }
  }

  private static readonly JsonSerializerOptions JsonOptions = new() {
    DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
    Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower),
                   new ByteArrayJsonConverter() },
  };

  /// <summary>Options for serializing config/input objects to FFI. Strips nulls
  /// (nullable C# fields default to null and would override required Rust
  /// fields with non-deserialisable nulls) but preserves explicit false/0 so
  /// caller intent is kept.</summary>
  private static readonly JsonSerializerOptions JsonSerializationOptions =
      new() {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(
                           JsonNamingPolicy.SnakeCaseLower),
                       new ByteArrayJsonConverter() },
      };
}
